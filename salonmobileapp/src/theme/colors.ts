/** @deprecated Import from @/theme/tokens — kept for backward compatibility. */
export {
  colors,
  spacing,
  radii,
  shadows,
  typography,
  touchTargetMinPx,
  palette,
} from "@/theme/tokens";
