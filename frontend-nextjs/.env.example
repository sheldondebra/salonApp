NEXT_PUBLIC_APP_URL=http://localhost:3000
# Optional: set only if not using the built-in Next.js proxy (see next.config.mjs)
# NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WORKPLACE_HOST=workplace.localhost:3000
NEXT_PUBLIC_ROOT_DOMAIN=localhost
# Local dev for /book-custom when not using a real CNAME host
NEXT_PUBLIC_DEMO_TENANT_SLUG=luxe-bloom
